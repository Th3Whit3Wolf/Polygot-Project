#include <iostream>

int main() {
    // A comment, this is so you can read your program later.
    // Anything after the // is ignored by the compiler.

    std::cout << "I could have code like this." << std::endl; // and the comment is ignored

    // You can also use a comment to "disable" or comment out code:
    // std::cout << "This won't run." << std::endl;

    std::cout << "This will run." << std::endl;

    return 0;
}